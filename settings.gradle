dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warni
        maven { url 'https://jitpack.io' }// ng: this repository is going to shut down soon
    }
}
rootProject.name = "Spotter"
include ':app'
